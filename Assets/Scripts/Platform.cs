// Importando bibliotecas necessárias
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Definindo a classe Platform que herda de MonoBehaviour
public class Platform : MonoBehaviour
{
    public Transform point;  // Referência a um ponto de transformação

    // Não há funções adicionais neste script
    // A classe Platform pode ser usada para armazenar informações específicas da plataforma,
    // como um ponto de destino (point) que pode ser atribuído no Unity Editor.
}